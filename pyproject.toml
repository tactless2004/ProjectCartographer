[project]
name = "Langoscope"
version = "0.1.0"
description = "Tool for generating git project breakdowns"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "jinja2>=3.1.6",
]
